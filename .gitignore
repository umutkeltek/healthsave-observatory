.env
.DS_Store
__pycache__/
.pytest_cache/
.ruff_cache/
.venv/
*.egg-info/

# Bun / TypeScript
node_modules/
dist/
*.tsbuildinfo

# Local-only compose overrides (per-machine ports, etc.)
docker-compose.override.yml

# Internal planning and agent docs - not for the public repo.
# docs/ is now PUBLIC (tracked); internal docs live in docs_private/.
docs_private/
agents.md
AGENTS.md
